
- Pool / delivery automation changes (bothook):
  - Added **cutover reconciler**: `BOTHOOK_CUTOVER_RECONCILER=1` worker in `control-plane/api-server.mjs` + systemd `bothook-cutover-reconciler.timer` (60s) to advance linked+paid+key_verified users to DELIVERED via `tryCutoverDelivered()` (and fixed `isPaid()` to treat DELIVERED as paid). Commit: 264ba53.
  - Fixed web + p-site click tracking: `WEB_CTA_CLICK`, `P_QR_CLICK`, `P_RELINK_CLICK` were 0 because listeners were bound in `<head>` before DOM existed; switched to **event delegation** across bothook-site + p-site (all locales) and deployed to `/var/www/*`. Commit: 7711b72.
  - Improved `/api/ops/pool/wa-sanitize`: now also logs out OpenClaw WhatsApp + clears `/home/ubuntu/.openclaw/channels/whatsapp` and `/home/ubuntu/.openclaw/credentials/whatsapp` (best-effort) in addition to clearing provision data dir. Commit: 3a5d610.
  - Pool safety + automation:
    - Allocator now only uses `IN_POOL` **and** `health_status='READY'` (prevents assigning DIRTY/NEEDS_VERIFY machines).
    - Watchdog reclaim marks `meta.provision_ready=false` when returning to pool.
    - Added **pool-cleaner** timer: `bothook-pool-cleaner.timer` (60s) runs `BOTHOOK_POOL_CLEANER=1` worker to call `wa-sanitize` for `IN_POOL` + (DIRTY/NEEDS_VERIFY) and mark READY on success.
    - Added Telegram alerting for pool-cleaner after consecutive failure runs (`BOTHOOK_POOL_CLEANER_ALERT_AFTER` default 3, cooldown default 30m) persisted in `control-plane/data/pool_cleaner_alert_state.json`. Commits: c957296, 8fc005f.
  - Root cause for slow QR generation: instance-side `bothook-openclaw-watchdog.timer` runs `/opt/bothook/bin/openclaw_watchdog.sh` which restarts `bothook-provision.service` after repeated `/healthz` failures; this can restart provisioning mid-QR and cause multi-minute waits. (Condition-triggered recurring issue, not pure fluke.)

- Stripe subscriptions mismatch fix:
  - Found webhook upsert bug: code attempted to insert `subscriptions.created_at`, but SQLite schema lacked column, so insert failed silently (try/catch). Verified via Stripe API that checkout.session.completed events had `mode=subscription` and `subscription=sub_...`.
  - Ran DB migration on `control-plane/data/bothook.sqlite`: `ALTER TABLE subscriptions ADD COLUMN created_at TEXT` + backfill `created_at=updated_at`. Updated `control-plane/schema.sql` accordingly and added `SUB_UPSERT_FAILED` event logging in webhook handler. Commit: fa4c6f7.
  - Backfilled 2 missing subscription rows by fetching Stripe events using `deliveries.meta_json.stripe_event_id`.
  - Added lightweight **subscription reconciler**: systemd `bothook-subscription-reconciler.timer` (10m) runs `BOTHOOK_SUB_RECONCILER=1` worker to backfill missing subscription rows from Stripe events (limit 3/run, last 7 days). Commit: 9f4ecdb.

- Investigation: oldest pool instance **43.166.132.60 / lhins-ly7sc7bo** “not selling” root cause (conversion funnel, last ~48h window anchored to max(events.ts)):
  - Counted sessions by `PROVISION_REALLOCATED(instance_id=lhins-ly7sc7bo)`:
    - sessions: **28**
    - `WA_LINKED` (scan/bind success): **8**
    - `PAY_OPEN`: **6**
    - `PAYMENT_PAID`: **0**
    - many end in unpaid reclaim/recycle (`DELIVERY_HARD_UNPAID_RECLAIM` / `RECYCLE_UNPAID`)
  - The 6 `PAY_OPEN` deliveries on this instance (all had `WA_LINKED`, none had any `PAYMENT_*` events):
    - uuid `2758f307-fc7b-4f96-868b-07bf27cbb39c` delivery `4c28760f-317a-4641-ad7b-a9d43a210123` (PAY_OPEN x2) → `RECYCLE_UNPAID`
    - uuid `c37be2cb-359b-46d1-9f57-729f8c057ee0` delivery `f3ec1394-48c0-44a4-a54b-a482b04c7d43` (PAY_OPEN x3) → `RECYCLE_UNPAID`
    - uuid `4da14dcb-2fa4-45b0-86ac-3987e4b16e23` delivery `97c3b2e7-dd9b-4a64-8c3c-cdd2f1b357da` (PAY_OPEN x1)
    - uuid `0c666e6f-03e5-4b63-89a0-33fdfb592cc9` delivery `a0df5cce-b8b0-4858-84f4-8a0301ab984d` (PAY_OPEN x1)
    - uuid `3d18ab55-8614-4cc3-8a8d-590db73f7ef5` delivery `b0162e4c-b169-4c7b-aef5-aec3988919c8` (PAY_OPEN x1)
    - uuid `54c450cb-c292-4e24-9263-21a0faf8d27d` delivery `5e2630f9-eb31-4cae-9137-dea2632851e1` (PAY_OPEN x1) → `DELIVERY_HARD_UNPAID_RECLAIM` (POST_BIND)
  - Conclusion: not primarily a QR/scan failure; bindings succeed and checkout opens, but payment completion is zero for these sessions (webhook overall is working; system had `PAYMENT_PAID` events in same 48h window).
