# 2026-03-10

- Google Ads API access (owner creds stored in /home/ubuntu/.openclaw/credentials/google_ads.env).
  - Verified minimal read via Python client **google-ads==29.2.0** using **SearchStream**.
    - Without login_customer_id: PERMISSION_DENIED w/ message requiring login-customer-id when accessing client customer.
    - With login_customer_id (MCC) set to `9776571037`: SearchStream succeeded; campaigns returned.
  - Verified REST works using **v23** endpoint:
    - POST `https://googleads.googleapis.com/v23/customers/1577191627/googleAds:searchStream`
    - Headers required: `developer-token` + `login-customer-id` (MCC)
    - Response 200 OK with JSON results.
  - Note: “3 campaigns” are ENABLED; some `ad_group_ad` are PAUSED; enabling requires explicit target ad ids/names.
